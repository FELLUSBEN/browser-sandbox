from .bash import bash_pipeline
# TODO: add all pipelines that should be exposed to the user of your backend in the import statement above.

pipelines = {
    "bash_pipeline": bash_pipeline,   # TODO: adapt identifier to something approproiate
}