from .pySigma-backend-bash import pySigma-backend-bash_pipeline
# TODO: add all pipelines that should be exposed to the user of your backend in the import statement above.

pipelines = {
    "pySigma-backend-bash_pipeline": pySigma-backend-bash_pipeline,   # TODO: adapt identifier to something approproiate
}